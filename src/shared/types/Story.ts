import type {StoryObj} from '@storybook/react-native'
import type {Meta} from '@storybook/react-native-web-vite'

/**
 * Story
 * Type definition for the component Storybook stories.
 */
export type Story<ComponentMeta extends Meta> = StoryObj<ComponentMeta>
