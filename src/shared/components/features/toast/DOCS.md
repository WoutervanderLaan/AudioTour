# src/shared/components/features/toast

Toast notification system.

## Purpose

Provides a complete toast notification system with:

- Toast component for displaying messages
- Queue management for multiple toasts
- Auto-dismiss with configurable duration
- Different toast types (success, error, info, warning)
- Animation and positioning

Works with `ToastContext` from `shared/context/` for global toast management.
