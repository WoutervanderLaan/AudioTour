# src/modules/auth/hooks

Custom React hooks for authentication functionality.

## Files

- **useAuth.ts** - Main authentication hook providing login, logout, register, and auth state access

## Purpose

Encapsulates authentication logic in reusable hooks that components can use to access auth state and perform auth operations. Provides a clean API for interacting with the auth store and API.
