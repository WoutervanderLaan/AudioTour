/**
 * BlurBackgroundProps
 * Props for the BlurBackground component
 */
export type BlurBackgroundProps = {
  /**
   * featherEdge
   * Specifies which edge should have a feathered (gradient fade) effect
   */
  featherEdge?: 'top' | 'bottom'
}
